PORT=3000
NODE_ENV=production

# MongoDB container configuration
MONGO_INITDB_DATABASE=products_db
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=256831quynh

# Connection string from web container to MongoDB container
MONGO_URI=mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@mongodb:27017/${MONGO_INITDB_DATABASE}?authSource=admin

# Docker Hub image (thay yourusername bằng username Docker Hub của bạn)
#IMAGE_NAME=yourusername/devops-app
IMAGE_NAME=zuinq/devops-app
IMAGE_TAG=latest

# Port mapping on host
#HOST_PORT=3000
HOST_PORT=8080

# Upload path inside container
UPLOAD_PATH=/app/public/uploads
MAX_FILE_SIZE=10485760

# Volume names
MONGO_VOLUME=mongodb_data
UPLOADS_VOLUME=uploads
